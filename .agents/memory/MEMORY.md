- [Adding a marketing/footer page](adding-marketing-page-surfaces.md) — new top-level info page touches ~10 surfaces (footer link, \_app TopNav exclusion, meta map, sitemap, proxy AGENT_VIEW_PATHS, PAGE_CONTENT, self-host blocklist, llms.txt x2, 3 mirrored test lists); each fails silently.
- [Relay-outage cached-first imagery](relay-outage-cached-first-imagery.md) — profile/shop/product fetches seed from DB first; relay fetches need resolveOnTimeout+bounded timeout and must never reject into callers; editShopContext REPLACES the map (never `new Map()` fallback).
- [Live shipping quote staleness](live-shipping-quote-staleness.md) — input-key each Shippo quote and re-validate at the consumption site, or a stale rate can be charged mid-debounce; FX lag is safe by design.
- [Lightning invoice polling loop](lightning-invoice-polling.md) — every branch inside the polling while-loop must advance retryCount and back off, and the loop must surface a terminal UI even when it exits naturally.
- [Lightning mint amount source](lightning-mint-amount-source.md) — mintProofsBolt11 amount must come from the exact value used to create the quote, not from a separate price state variable that can diverge under payment-method discounts.
- [Cashu post-swap recovery](cashu-post-swap-recovery.md) — every safeSwap/safeMeltProofs caller must stash the new UNSPENT outputs on any post-swap throw, or the wallet keeps SPENT inputs as a phantom balance.
- [Payment overlay cleanup](payment-overlay-cleanup.md) — long-running payment overlays must clear their visibility flag in `finally`, not `catch`; non-dismissable modals turn asymmetric cleanup into a hard-block.
- [Post-payment redirect router churn](redirect-effect-router-churn.md) — a timed router.push effect must read router via a ref (not in deps) + a scheduled-once ref, else router-identity churn re-arms the timer and the confirm GIF loops forever.
- [Payment-method group surfaces](payment-method-group-surfaces.md) — seller button reorder + Bitcoin opt-out threads through domain, seller-normalize, form, BOTH checkout cards, and MCP; buyer hides bitcoin only when card/fiat exists.
- [Multi-mint wallet attribution](multi-mint-wallet-attribution.md) — proofs carry no mint; attribute via keysets, promote receiving mint to default, pick spend mint by probe, dedup reactive reloads, and retry loadMint on failure or balances go stale/zero.
- [Cashu spent-proof self-heal sweep](spent-proof-self-heal.md) — wallet must periodically checkProofsStates and prune SPENT local proofs, with merge-safe writes and a per-tab lock so it can't delete funds added mid-probe.
- [Shipping shipment claim + dedup](shipment-purchase-claim-race.md) — shipment ownership + dup-buy guard share one Postgres table via atomic claim; release on failure, canonical idempotency key for returns.
- [Storefront SSR head tags](storefront-ssr-head.md) — seller favicon/OG meta AND Product/ItemList JSON-LD must flow through getServerSideProps ogMeta → DynamicHead; client-only head tags are invisible to crawlers; grep for a pre-existing client JSON-LD copy to avoid duplicate nodes.
- [Storefront overlay theming](storefront-overlay-theming.md) — modals/dropdowns portal to [data-overlay-container]; themed by a SEPARATE `body.sf-active` allowlist, easy to leave incomplete vs in-page .storefront-themed. sf-active also hides [data-main-nav] globally — only set it when seller chrome actually renders.
- [Reported vs charged amounts](reported-vs-charged-amounts.md) — order totals in DMs/email/dashboard must equal items+discounted-shipping (display-only), computed separately from fund amounts (ecash proofs/Stripe charge).
- [Platform fee on pre-tax base](platform-fee-pretax-base.md) — donation/application_fee must be computed on items+shipping, never items+shipping+tax; sales tax is collected for the seller to remit, so fee'ing it shorts them.
- [Order DM seller-relay delivery](order-dm-seller-relay-delivery.md) — order gift-wraps must reach the SELLER's relays (server republish of the signed event needs no key + client NIP-65 fallback), not the buyer's; else custom-domain orders miss the dashboard.
- [Storefront deferred chat load](storefront-deferred-chat-load.md) — signed-in custom stall/domain pages must defer gift-wrap message decryption off the render path; guard deferred chat writes with a run token bumped on slug change AND auth change.
- [Independent side-effects in handlers](independent-side-effects-in-handlers.md) — submit handlers doing email + Nostr publish + DB write must fire the email first and isolate each effect in its own try/catch, or one throw silently skips the rest.
- [Order participants not server-readable](order-participants-not-server-readable.md) — encrypted wraps cannot prove buyer/seller roles; neither null participants nor outer gift-wrap tags are safe authorization fallbacks.
- [Flow enroll + schedule atomicity](flow-enroll-schedule-atomicity.md) — enrollInFlow and scheduleStepExecutions aren't transactional; on scheduling failure cancelEnrollment to avoid stranding contacts as active-with-no-executions (dedup skips active forever).
- [Order spec descriptors threading](order-spec-descriptors.md) — a per-line spec must be wired into ~10 surfaces or it vanishes from some views; variants stay descriptive-only (no price/inventory).
- [Parcel tag is a checkout contract](parcel-tag-checkout-contract.md) — live rates need BOTH parcel tag AND a ship_from_zip origin; republish gap-fills origin from seller defaults or checkout silently uses the fixed rate.
- [Spec shipping options (kind 30406)](spec-shipping-options-30406.md) — overlay not replacement: legacy shipping tag stays (cart/fallback), spec option wins the invoice-card cost seam; country exclusion is a hard checkout block, region/weight/dim advisory; edit hydration preserves unresolvable refs.
- [Pasted PEM keys arrive newline-collapsed](pem-secrets-collapse-newlines.md) — secret-form pastes can land as one space-joined line; normalize PEM (quotes/literal-\n/space-collapse) at the consumption site, never print key material to diagnose.
- [Upstream→milk-market merge strategy](upstream-merge-strategy.md) — never bulk-merge shopstr upstream; cherry-port only refinements, restyle to neo-brutalist, skip files reworked downstream.
- [Cashu API-drift guardrail](cashu-api-contract-guardrail.md) — mocked Send/Mint tests stay green when @cashu/cashu-ts renames methods; a real-library contract test is the only thing that catches the drift.
- [Jest transformIgnorePatterns under pnpm](jest-pnpm-transformignore.md) — one optional-group ESM-allowlist regex backtracks and wrongly ignores nested @noble/curves ESM; use two explicit patterns (pnpm + classic) or real-nostr-tools tests crash.
- [Upstream parity + dev OOM](upstream-parity-and-dev-oom.md) — record git parity with upstream via `merge -s ours` (hand-off, not main agent); dev server dying after pnpm install is a 1GB cold-compile OOM, not a regression.
- [Tests must not live under pages/](nextjs-tests-under-pages.md) — a Jest test colocated in pages/ builds as a route and crashes `next build` with "expect is not defined" (dev is fine); keep tests in top-level **tests**/.
- [Authed-sellers marketplace gate](authed-sellers-marketplace-gate.md) — marketplace shows only pubkeys in server-side authed_sellers (grown on password entry); filter must fail closed; recorded pubkey is client-supplied (trust = shared password).
- [Onboarding flow redirects](onboarding-flow-redirects.md) — inserting a wizard step means updating every redirect into it (incl. two Shopify-migrate auto-redirects), not just the linear Next chain.
- [Pro feature enforcement surfaces](pro-feature-enforcement-surfaces.md) — gate server-backed Pro features (domain/MCP/email) at the write endpoint; Nostr-published design has no server write path, enforce at the serve/render layer.
- [Stripe webhook claim release](stripe-webhook-claim-release.md) — claimStripeEvent permanently dedups; release the claim in catch on handler failure or Stripe's retry is silently dropped.
- [DB lookups must not swallow outages as null](db-lookup-null-vs-outage.md) — webhook-consumed accessors must throw on DB error; null = genuinely missing (loud marker + 200), throw = 500 + claim release for Stripe retry.
- [One-shot idempotency claim ordering](idempotency-claim-ordering.md) — take the once-per-key claim AFTER all skip/empty-audience early-returns, or a no-op attempt permanently burns it and the version can never run.
- [Membership extension atomicity](membership-extension-atomicity.md) — stack paid time in one SQL stmt from GREATEST(now, current end), never read-modify-write, or concurrent settles drop a term.
- [ON CONFLICT granters need a created flag](conflict-insert-created-flag.md) — one-time INSERT...ON CONFLICT DO NOTHING grants must return whether a row was created; gate success UI on it, not on a 200, or stale clients fake the grant.
- [Lifetime grant vs live Stripe sub](lifetime-grant-stripe-cancel.md) — grantLifetimeMembership only nulls the local sub id; call cancelExistingProSubscription FIRST or the seller keeps getting charged at Stripe.
- [MCP create_order tool/route contract](mcp-create-order-contract.md) — the create_order tool schema and the create-order route are separate contracts; thread any new/required field through tool schema+destructure+body or agents can't supply it. Recurring orders are Stripe-only and reuse /api/stripe/create-subscription.
- [MCP tier enforcement & stall parity](mcp-tier-and-stall-parity.md) — lapsed sellers' MCP keys are deactivated (not deleted, FK) at lifecycle readonly/hidden transitions; set_shop_profile must mirror every StorefrontConfig field.
- [Lifetime membership vs subscription webhooks](lifetime-membership-and-subscription-webhooks.md) — keep `lifetime` a sticky column resolver-checked first; guard the sub→membership mapper to skip+cancel for lifetime members so renewals can't clobber/double-charge.
- [Next 16 proxy.ts + content negotiation](next16-proxy-content-negotiation.md) — Next 16 uses proxy.ts not middleware.ts; NextResponse.rewrite drops dest query (forward via request headers); honor mixed Accept for markdown/json.
- [Landing mobile "overflow"](landing-mobile-overflow.md) — globals force body overflow-x:hidden, so overflow complaints = fixed-width clipping; cap carousel cards at w-[min(20rem,calc(100vw-Xrem))] like the storefront social section.
- [Jest haste-map must exclude .next](jest-cold-compile-tool-window.md) — next/jest ignores .next for test/watch but NOT the haste-map crawl, so the dev `next build` makes jest crawl .next/standalone → ENOENT crash + slowness; unit jest IS agent-runnable, real-Postgres testcontainer tests aren't.
- [Machine-readable marketing/FAQ surfaces](machine-readable-tier-surfaces.md) — tiers: page-content.ts + llms-full.txt; FAQ/fee claims live in 3 JSON-LD copies and homepage JSON-LD ≠ visible FAQ.
- [Wrangler lifetime price change checklist](wrangler-lifetime-price-change.md) — change the constant AND bump the Stripe lookup_key (Prices are immutable); charge uses the constant directly, Price is cosmetic; update copy + tests.
- [Agent-readiness scanner gaps](agent-readiness-scanner.md) — agent 404s route through tryWriteAgentNotFound; WBA dir needs literal application/json; keyless MCP sessions need atomic pending-slot caps.
- [GEO shipping destination from currency](geo-shipping-destination.md) — Google needs a shippingDestination to show shipping cost; derive region from shipping currency (single-country map), omit ambiguous (EUR)/bitcoin, never fabricate.
- [Web Bot Auth signature directory](web-bot-auth-directory.md) — publish Ed25519 JWKS at /.well-known/http-message-signatures-directory (top of proxy = both hosts); key rotates per-process unless env set; use res.send not res.json for the media type.
- [Monorepo package tsconfig @types](monorepo-package-tsconfig-types.md) — each packages/\* tsconfig must set `types: ["jest","node"]`; TS6+pnpm skips auto-@types; @types/node has fetch/Headers but not BodyInit.
- [pnpm injected workspace deps](pnpm-injected-workspace-deps.md) — editing a packages/\* source won't update apps/mobile typecheck until `pnpm install --ignore-scripts` resyncs the injected copy; restore the committed lockfile after.
- [Stripe-connect auth fallback binding](stripe-connect-auth-fallback.md) — generic "stripe-connect" verifyNostrAuth fallback = cross-endpoint replay vector; bind write endpoints with expectedBinding{method,path}; primary MCP proof is the real auth.
- [Sales tax is card-only](sales-tax-card-only.md) — Stripe tax (on by default) is a separate add-on on the card button only (LN/Cashu/fiat untaxed), never folded into order totals.
- [FX display vs charge resilience](fx-display-vs-charge-resilience.md) — buyer-facing FX displays use \*Resilient wrappers (retry+bounded-freshness, return null); charge math stays fail-closed (throws). Never mix.
- [Stripe Connect disconnect/reconnect](stripe-connect-disconnect.md) — reconnect needs the stripe_connect_accounts row DELETED first (create-account short-circuits on existing); disconnect is unlink-only, never delete the account at Stripe.
- [Email sender-domain fail-closed fallback](email-sender-domain-fallback.md) — custom seller from-address used only when valid+host==domain; sendEmail retries global sender on 403.
- [Paywall every sibling endpoint](paywall-sibling-endpoints.md) — gating a feature's named endpoint/UI isn't enough; grep the DB accessor for sibling READ routes (old open `?seller_pubkey=` ones leak cross-seller) and gate them too; keep tracking WRITE endpoints public.
- [Flow email deep-link gotchas](flow-email-deeplink-gotchas.md) — links in flow emails are click-wrapped, so an inner deep-link token's TTL must stay ≤ the 90d click TTL; auto-open must bind matched order to the token's seller (orderId isn't unique across sellers).
- [Trackable email-flow CTA links](trackable-email-flow-links.md) — rewrite links AFTER render (decode/re-escape entities) only on real send path; redirect 302s only to the HMAC-signed dest, never query input.
- [Storefront product-as-landing reuse](storefront-product-landing.md) — serving a product at the stall/domain root must reuse ProductListingView + eventToProductOgMeta (no second copy); gate is render-layer fail-closed via basicStorefront().
- [Listing-tag URL sanitization](listing-tag-url-sanitization.md) — any buyer-rendered URL from a permissionless listing tag must be http(s)-validated at parse AND sanitizeUrl'd at render; seller form only makes safe URLs but the event is attacker-controllable.
- [HeroUI Button as=label swallows file clicks](heroui-button-label-file-upload.md) — hidden file input via `Button as="label" htmlFor` won't open the picker (react-aria eats the click); use ref + explicit input.click() like file-uploader.tsx; add `multiple` + per-file try/catch for batches.
- [Stripe receipt_email ≠ recipient ownership](stripe-receipt-email-not-ownership.md) — card-verified custom-from order confirmations block free spoofing but a paying attacker can still target one chosen recipient per charge; receipt_email is caller-supplied.
- [Flow email render entry points](flow-email-render-entry-points.md) — flow + one-time emails render in process.ts (styled by default); test sends render separately in send-test.ts, so any flow-email change must touch BOTH paths.
- [Storefront section dual sanitizer](storefront-section-dual-sanitizer.md) — StorefrontSection has TWO sanitizers (homepage sections[] = full fields; pages[].sections[] = id/type/heading/body only) and SectionEditor is shared, so new hero fields silently vanish on custom pages.
- [Adding a StorefrontSectionType](adding-storefront-section-type.md) — new section type touches ~8 surfaces; compiler catches the type set + SECTION_LABELS Record, but SectionPreviewSvg's default:null preview, SECTION_PLACEHOLDERS blank-preview, and sanitizer allowlists fail silently — grep an existing type.
- [Storefront footer editor surface](storefront-footer-editor-surface.md) — footer customization (newsletter/layout parity) has its OWN footer-editor.tsx; wiring domain+renderer+MCP but skipping it means the human settings UI can't configure the feature (MCP-only).
- [Marketing screenshots of milk.market](marketing-screenshots-capture.md) — only SSR'd /stall/[slug] renders products via Firecrawl; /marketplace & /listing capture as spinners; auth/modal screens NEED a controlled Puppeteer browser (NIP-49 localStorage inject), not the external tool.
- [Dev storefront config verification](dev-storefront-config-verification.md) — live-test storefront features by editing the cached kind:30019 profile_events row + marker name + loud navColors; restore after.
- [Self-host card payment fail-closed](self-host-payment-failclosed.md) — self-host card-charge authorization lives in the payment-intent route (own-Stripe + tenant-pubkey gates), not the card-availability UI; hiding a button isn't authorization.
- [Self-host header trust env-gated](self-host-header-trust.md) — never trust the spoofable x-ss-self-host header alone; gate it on the server SS_SELF_HOST env (pure helper in routing.ts) or forceSelfHostChrome bypasses the hosted Pro render gate.
- [Storefront lookup resilience](storefront-lookup-resilience.md) — cache only HTTP 404 negatives (retry transient); bind client lookup state to route identity (kind:value) + gate on router.isReady, else stale-domain renders / premature not_found.
- [Dynamic NIP-05 on custom domains](dynamic-nip05-domain-resolution.md) — /.well-known/nostr.json route must resolve seller from verified domain + membership gate, never a forgeable pubkey header (publicly reachable = no header trust).
- [Profile local-fallback ordering](profile-local-fallback-ordering.md) — auto-edits to the kind:0 profile must publish FIRST, then write the localStorage fallback; settings form prefers the newer fallback, so a failed publish causes silent false-success drift.
- [Single-flight passphrase challenge](concurrent-signer-challenge.md) — provider stores ONE challengeResolver; concurrent signs clobber it and hang forever. Now backstopped by single-flight `_getPrivKey` (coalesces one in-flight unlock); still serialize mount-time signs per-page and thread gating via return values.
- [Gift-wrap chat fetch resilience](gift-wrap-chat-fetch-resilience.md) — fetchGiftWrappedChatsAndMessages must NEVER reject (callers blank the map), isolate each wrap decrypt, treat relay fetch as non-fatal, publish cached-first then merge (fresh Map each time), dedup by wrap id.
- [Storefront community fetch resilience](storefront-community-fetch-resilience.md) — storefront community fetch must seed from DB FIRST (own try/catch) like fetchAllCommunities; relay-first ordering let a relay error wipe DB-cached community to empty (blank stall tab, marketplace still showed it).
- [Task-merge resets diff base](task-merge-diff-base.md) — after a task-agent merge HEAD drops below your commits, so inherited work looks uncommitted and architect includeGitDiff misleads; diff via `git show <lastCommit>:file`.
- [Per-stall content negotiation surfaces](stall-content-negotiation-surfaces.md) — a new agent-readable stall/post view must be wired into all 3 proxy host branches (platform/custom-domain/self-host) + the backing stall-agent-view endpoint, threading params via headers AND query.
- [Replit edge XFF single trusted hop](replit-edge-xff-trust.md) — verified live: edge appends real client IP as rightmost x-forwarded-for, un-spoofable; per-agent rate-limit budgets isolate; trust rightmost token, no x-real-ip fallback needed; prod responses are cache-control:private.
- [Rate limiter shared store + fail-open](rate-limit-shared-store.md) — limiter counts in Postgres (cross-instance) with in-memory fallback; checkRateLimit/applyRateLimit are async (await everywhere); fail-open on store error; mock db-service to test limit-walking; 429 body {error,code,retryAfterSeconds} is canonical (delete-events' bare {error} test is pre-existing-stale).
- [Source-targeted email audience](source-targeted-audience.md) — getSellerAudienceEmails(pubkey, source?) drops the buyers union when a popup/subscription source is given; broadcast route validates + auth-binds audienceSource; flow picker filters client-side via getPopupEmailCapturesBySeller's source.
- [Auto-action-after-payment replay safety](auto-label-replay-safety.md) — unauthed server actions fired after a card payment must dedupe on the verified PaymentIntent id (claimRef), never a client orderId, and never prune the permanent claim marker, or one settled PI buys unlimited seller-billed labels.
- [Square Apple Pay SDK contract](square-apple-pay-sdk-contract.md) — no attach(): render own button + tokenize() on click; resolution = availability check; wallet total = toSmallestUnit; fiat-only.
- [Alt per-seller card processor (Square)](square-alt-card-processor.md) — config-gate must require EVERY behavior-changing env (incl. SQUARE_ENVIRONMENT) not just creds or it defaults to sandbox; autocomplete charge success = COMPLETED only (APPROVED = uncaptured); XOR enforced at both write endpoints.
- [Stripe Connect regional support](stripe-connect-regional.md) — account country is immutable so create-account fails closed (country_required); EVERY client (web, mobile, MCP, api-client) must send it; tax registrations region-shaped per country (US state / CA province / whole-country); Square crypto → satsToFiat(location currency).
- [Stripe Standard Connect (OAuth)](stripe-standard-connect.md) — seller-owned accounts via OAuth; HMAC state is the only callback binding; account_type column; callback upsert REPLACES express rows (migration path); Express-only APIs must reject standard accounts.
- [Agent bash sandbox gotchas](agent-sandbox-bash-gotchas.md) — never `pkill` (kills agent shell); `setsid`+poll for long builds, but detached tsc still gets reaped + full tsc is very slow here — use LSP getLatestLspDiagnostics per file for type checks.
- [Marketplace discovery tag write paths](marketplace-discovery-tag-writes.md) — every kind-30402 write path must normalize tags via normalizeMarketplaceDiscoveryTag; unconditional ["t","SelfSown"] appends after user-controllable categories silently duplicate/keep legacy tags.
- [Product context client-side dedup](product-event-client-dedup.md) — client mutations (addNewlyCreatedProductEvent) must upsert kind:30402 by pubkey:d-tag via the shared helper, never append by event id, or a republish duplicates the listing (featured banner + card).
- [Storefront email-popup field threading](storefront-email-popup-fields.md) — popup config has NO sanitizer (unlike sections), so new fields round-trip via type+settings+render; but MCP set_shop_profile rebuilds emailPopup lossy (drops style/flowSteps/displayMode).
- [Listing password gate = anti-bot friction](listing-password-gate.md) — LISTING_PASSWORD is intentionally NOT security; endpoint returns it verbatim to display in the prompt; don't "fix" as a leak; same modal duplicated in 3 surfaces (stall-feed, shop-profile-form, community) that must stay in sync.
- [Website-import (/convert) pipeline](site-import-enrichment.md) — richer imported sections must be built deterministically (AI never emits URLs); only ONE `about` (AI clobbers copy); give text/image sections real content so the preview's placeholder-fill can't fabricate; shared builder feeds public + Pro import.
- [URL website-import mode threading](url-import-mode-threading.md) — stall-vs-product import mode must be threaded through client signed fields AND server authFields, gated so legacy stall signatures stay byte-identical; cache previews per `${mode}:${url}`.
- [Per-seller product read must dedup by address](per-seller-product-dedup.md) — product_events keeps every version; per-seller reads must DISTINCT ON (pubkey,d-tag) latest like the marketplace read, or storefronts show stale dupes.
- [Public server-side URL fetch hardening](public-url-fetch-hardening.md) — making an authed URL-fetcher public needs stream+body-cap with its own read deadline, per-IP + global bucket, and per-URL cache.
- [SSRF guard must decode mapped IPv6](ssrf-mapped-ipv6.md) — route every server fetch of a user URL through utils/url-safety safeFetch; private-IP checks must decode ::ffff: IPv4-mapped/compatible IPv6 or a DNS AAAA record bypasses them on dual-stack; DNS-rebinding TOCTOU is a known residual.
- [shop-profile-form triple load path](shop-profile-form-load-paths.md) — form hydrates from racing fast(DB)/slow(relay)/timeout paths; post-load mutations (e.g. URL-import draft) need a run-once ref set before clearing source AND the slow-path override must early-return on it or it silently clobbers.
- [db-service mocks need getDbPool](db-service-mock-getdbpool.md) — utils/db/\* call getDbPool() at module scope; a db-service mock without it kills the whole suite at import time (blocks pre-commit findRelatedTests).
- [Runtime schema bootstrap](runtime-schema-bootstrap.md) — hosted dev+prod schema is built at runtime by initializeTables() in db-service.ts, NOT db/schema.sql (self-host only) nor publish-diff; add every new table/column there or the endpoint 500s "column does not exist"; apply DDL to dev directly + re-publish for prod.
- [Cross-process runtime DDL coordination](schema-ddl-advisory-lock.md) — serialize runtime DDL with one pg_advisory_xact_lock inside a single explicit transaction; session-level locks misfire through transaction poolers (Neon -pooler).
- [Unstable prop defaults render loop](cart-fx-test-render-loop.md) — `= {}` destructuring defaults in effect deps loop renders when tests omit the prop; use a frozen module-level STABLE_EMPTY_OBJECT default.
- [Replit AI blueprints scaffold Express/Drizzle](replit-blueprint-scaffolding-mismatch.md) — delete Express/Drizzle scaffolding; wire the LLM via one guarded llm-json seam; Replit's Claude proxy rejects assistant prefill — use brace-slice.
- [Cashu wallet boot-refresh clobber race](cashu-wallet-boot-refresh-race.md) — a wallet refresh that snapshots localStorage then awaits seconds of work must delta-merge current tokens back (minus proven-spent secrets) at the write site AND every write site must skip an empty result over a non-empty wallet, or a concurrent send/melt's change gets clobbered to zero.
- [Restore-from-backup must verify unspent](cashu-restore-verify-unspent.md) — restoring kind-7375 proof events must group by issuing mint, verify UNSPENT via filterUnspentProofs, and fail-closed skip unreachable mints (report skippedCount), or spent proofs reappear as phantom balance; callers await + surface "try again".
- [Cashu send-token ledger + reclaim](cashu-send-token-ledger.md) — record every Send token in the durable ledger BEFORE display (fail-closed stash on write failure); reclaim must swap via wallet.receive for fresh secrets, never re-persist original proofs; no Copy on reclaimed entries.
- [Real-Postgres tests in sandbox](real-postgres-test-pattern.md) — testcontainers can't bind ports here; dual-gate real-DB tests (RUN_TESTCONTAINERS or \*\_TEST_DATABASE_URL) and run against dev DATABASE_URL with synthetic-row cleanup.
- [Self-host full-build test in sandbox](self-host-build-test-sandbox.md) — RUN_SELF_HOST_BUILD suite DOES run here (~7min) despite its CI-only header comment: stop dev-server workflow first, use a scratch DATABASE_URL (env URL has no port — naive rewrites yield host:None).
- [Full Jest suite verification here](full-suite-jest-verification.md) — verify via three serial `--runInBand --shard=k/3` runs, not one parallel run; the always-on dev server starves parallel Jest → OOM (no output) or false findByRole/timeout flakes that pass in isolation; exit 124 after "Ran all test suites" is still a valid pass.
- [Renaming user-visible strings](renaming-user-visible-strings.md) — grep old labels across ALL test dirs incl. colocated `components/**/__tests__/`, not just root `__tests__/`; exact getByText breaks, `{exact:false}` is safe; preserve within(dialog) scoping for same-file duplicate labels.
- [Section element layout fields](section-element-layout-fields.md) — new per-section layout fields thread through shared sanitizer (both allowlists) + BOTH MCP zod schemas + SectionElementFlow with render-time safety (pageConfig path is unsanitized); unset fields must render byte-identical legacy markup.
- [Marketplace vendor-view gating](marketplace-vendor-view-gating.md) — single-vendor layout gates on focusedPubkey, never optional cosmetic fields (banner); SSR renders no-shop-profile vendors who have listings, notFound only when no profile AND no products.
- [Contact-form gate surfaces](contact-form-gate-surfaces.md) — contact*form lives in 4 surfaces (homepage sections, builder pages, productPageDefaults, per-product page_config); BOTH public endpoints must check all; never sanitizeFullSection productPageDefaults (downgrades product*\* to text; round-trip erased it).
- [OG image proxy seam](og-image-proxy-seam.md) — storefront og:image wrapped at DynamicHead via toOptimizedOgImageUrl → /api/og-image; route must stay in custom-domain proxy allowlist; route tests calling the handler repeatedly must mock @/utils/rate-limit (jsdom pg-pool degradation mimics logic bugs).
- [Dev workflow serves a production build](dev-workflow-is-production-build.md) — Next.js Dev Server workflow = next build + standalone, no hot reload; restart (2.5min) before any live verification or "stale" HTML mimics logic bugs.
- [Invoice card dual Order Summary panes](invoice-card-dual-order-summary.md) — product-invoice-card.tsx has TWO near-identical Order Summary blocks with different indentation; patch both or one payment path silently misses the change.
- [Blank string coerces to 0](blank-string-number-coercion.md) — Number("")==0: optional numeric string fields (MCP z.string(), forms) must reject blank as "unset" before Number(); share one builder helper across all write paths.
- [Mobile listing partial edits](mobile-listing-partial-edits.md) — mobile owns only basic tag families; preserve every other product tag, and keep relay-success retries on one monotonic address.
- [ZapSnag fiat settlement](zapsnag-fiat-settlement.md) — kind-1 listings display their listed fiat; convert to validated sats only at zap time, while order metadata stays fiat.
- [GitHub push via connector](github-push-via-connector.md) — connector has no raw token; push via Git Data API + shaMap then reset local to origin/main; WAF blocks literal XSS payloads in blob uploads; reauthor agent commits via rebase --exec amend --no-verify.
- [DNS-pinned Undici lookup](dns-pinned-undici-lookup.md) — custom lookups must honor the all-address callback shape; smoke-test real outbound fetches, not mocks alone.
- [Sharp in Next standalone](sharp-next-standalone.md) — pnpm tracing can omit Sharp package metadata/libvips despite a green build; verify and repair the nested runtime package context.
- [Cashu escrow outbox design rules](cashu-escrow-outbox-design.md) — one row/escrow + fencing tokens; worker must strict-check mint state every attempt, persist prepared outputs pre-swap (NUT-09 restore recovery), allowlist P2PK tags, convert expired releases, back off retries.
- [Cashu escrow custody rule](cashu-escrow-custody.md) — locked proofs never go to the seller (self-redeem risk); buyer custody fail-closed, never truncated; messages carry the escrow id, never the token; every pending outbox stage must stay advanceable, including past expiry.
- [Escrow backups are not wallet balance](escrow-backup-not-balance.md) — kind-7375 escrow-marked proofs must be excluded from EVERY spendable-wallet path and restored only into cashu_escrows.
- [cashu-ts v4 live-only requirements](cashu-ts-v4-live-requirements.md) — mocked tests hide v4's live-only requirements: v2 keyset retry, loadMint on all ops, unit-less receive rejection.
- [Staging Cashu mint bootstrap](staging-cashu-mint-bootstrap.md) — workflow self-heals the /tmp venv; firewall blocks h11<0.15; PIP_USER=0; no waitForPort on 127.0.0.1.
- [Staging escrow E2E seed coupling](staging-escrow-e2e-seed-coupling.md) — setup-script seed string deterministically derives the recovery harness's hardcoded naddr; rename the seed = re-derive the naddr in the same commit (grep can't see it).
- [Brand positioning: local food, not raw milk](brand-positioning-local-food.md) — public copy must lead with local food + artisan goods; raw-milk-first positioning is dismissed.
- [P2PK pubkey normalization](p2pk-pubkey-normalization.md) — mints emit P2PK lock data compressed (02+x-only), records carry x-only Nostr pubkeys; always compare via normalizeP2PKPubkey, and use the compressed form in test fixtures.
- [Escrow backup encryption surfacing](escrow-backup-encryption-surfacing.md) — encryption_failed is permanent for nip04-only bunkers; render it in a view the active payment path actually opens, never console-only.
- [Checkout-card component test recipe](checkout-card-component-test-recipe.md) — jsdom Cashu escrow recipe: sats pricing, N/A shipping, closure-deferred mock refs, v4 Amount fixtures + satPrices for cart.
- [Stripe subscription account binding](subscription-account-binding.md) — cancel/update must target the row's stored connected_account_id (stamped at creation); seller's current account is only the legacy-row fallback or reconnecting a different account orphans the sub.
- [Stripe webhook endpoint topology](stripe-webhook-endpoint-topology.md) — routes handling platform and connected-account objects need separate account+Connect endpoints and secrets; publish after secret changes.
- [Ops-alert per-day dedup](ops-alert-dedup.md) — repeatable ops alerts go through the shared pro_settings-backed dedup helper (per-key advisory lock); stamp ONLY after a confirmed send, never throw into webhooks.
- [Blog broadcast dedup ledgers](blog-broadcast-dedup-ledger.md) — per-segment version claims + immutable per-recipient ledger (capture source is mutable, membership exclusion re-emails after flips); zero-owned sends must release the version claim.
- [pg-mem harness quirks](pg-mem-harness-quirks.md) — ON CONFLICT conflict misreports rowCount=1 with phantom row (probe+override in wrapper); none() doesn't bind $n; pass-through regex must match ALL touched tables (prefix traps).
- [Jest fake timers fake queueMicrotask](jest-fake-timers-queuemicrotask.md) — in fake-timer suites, queueMicrotask callbacks never run; test doubles must defer via native Promise.resolve().then() or awaits deadlock into 5s timeouts.
- [Jest maps ALL .css resolution to styleMock](jest-css-resolve-stylemock.md) — even createRequire.resolve("pkg/x.css") in test code returns next's styleMock.js; load real CSS via package.json path + path.join.
- [Publish firewall CVE blocks](publish-firewall-cve-block.md) — publish failing at npm-install with 403 "Critical CVE" means the pinned dep version is firewall-blocked; bump the dep, don't debug app code; local builds keep working off the warm pnpm store.
- [Next build OOM contention](next-build-oom-contention.md) — cold-build OOM root cause: Tailwind v4 auto source detection scanning .local/.cache (fixed via source(none)+@source globs); never bare `pnpm run build` (OOMs even with GBs free) — verify via the Next.js Dev Server workflow log.
- [Expo tooling peer-expects TS 5](mobile-typescript-6-peer-warning.md) — TS 6.0.3 everywhere; @expo/require-utils TS^5 peer warning verified benign (expo config/export work); mobile tsconfig must not set `baseUrl`; zod missing v3/ = corrupted pnpm store, reinstall to repair.
- [Stale-chunk hydration kill](stale-chunk-hydration-kill.md) — rebuilds used to orphan old chunk hashes, killing hydration so HeroUI images stuck invisible; dev-server carries static forward, staged renames, 250MB cap; test.sh case F.
- [Orphaned SW registrations](orphaned-sw-registrations.md) — a SW path that 404s strands its old worker forever; serve a self-destruct worker at every historical SW path (public/sw.js, service-worker.js).
- [HeroUI theme must be a direct dep](heroui-theme-direct-dep.md) — Tailwind @source into node_modules silently no-ops for transitive deps under pnpm; publish builds lost ALL HeroUI slot classes (label float, image reveal).
- [Site URL centralization gotchas](site-url-centralization.md) — getSiteUrl returns env verbatim (no trim/strip — pure-refactor contract); JSX text/expression child boundaries affect SSR bytes; tests stubbing NEXT_PUBLIC_BASE_URL must assert literals, not import SITE_URL.
- [Legacy-domain cutover redirect](legacy-domain-cutover-redirect.md) — milk.market page traffic 301s to canonical host (path+query, port-stripped, /api + /.well-known exempt, suffix entry stays); no-op until base-URL flips; time-boxed.
- [Brand rename alias layer](brand-rename-keep-list.md) — rename to selfsown/self-sown is DONE; remaining milkmarket identifiers are deliberate compat aliases — never remove.
- [Editing .replit](editing-dot-replit.md) — direct Edit is blocked; write full TOML to a temp file and call verifyAndReplaceDotReplit in CodeExecution.
- [replit.nix channel caps Node at 18](replit-nix-channel-node-cap.md) — nixpkgs 22.11 has no nodejs-22_x and replit.nix is still evaluated at env-build; runtime Node 22 comes from .replit modules, never bump replit.nix to match .nvmrc.
- [Deploy-build publish guard](deploy-build-publish-guard.md) — deploy-build.sh refuses to run without SELF_SOWN_PUBLISH_BUILD=1, set only by the .replit [deployment] build command; keep the two in lockstep.
- [Security scanner accepted findings](security-scan-accepted-findings.md) — recurring SAST/HoundDog classes confirmed by-design; skip them and investigate only new fingerprints.
- [Apple Pay association routing](apple-pay-association-host-aware.md) — one processor's file per domain; marketplace host 404s by product decision, custom domains serve the seller's processor file.
- [Brand palette consolidation](brand-palette-consolidation.md) — app is forced light-only; keep darkMode:'class' (HeroUI needs it); never add dark: classes.
- [Tailwind v4 dead vs deprecated utilities](tailwind-v4-dead-utilities.md) — v4.3.3 drops *-opacity-* and re-scales *-sm sizes (pinned+guarded); deprecated aliases migrated; bare shadow/rounded/blur/ring stay.
- [Headless browser verification](headless-browser-verification.md) — no puppeteer here; use /repl/tools/bin/chromium headless for DOM/computed-style checks; kill by PID, never pkill.
- [Pro-status transient-failure policy](pro-status-failopen-cache.md) — pro-status gating must fail closed only on definitive isPro:false; transient failures retry + use last-known-good cache.
- [Mobile dependency security overrides](audit-override-unfixable-pins.md) — patched image-size needs a Metro filename→buffer shim; update query-string rather than forcing its ESM-only decoder under v7.
